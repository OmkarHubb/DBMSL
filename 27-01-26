create table DEPARTMENT (Dept_ID Number(30) NOT NULL, Name varchar(100) NOT NULL, Building varchar(50), Budget Number, University_ID number(30));

alter table department modify building varchar2(50) NOT NULL;

update instructor set salary = salary + salary * 10/100 where dept_id = (select dept_id from department where name = 'Computer Science');

update instructor set salary = salary + salary * 10/100 where dept_id = (select dept_id from department where name = 'Mathematics');

update instructor set salary = salary + salary * 0.5 where salary < 80000;

insert into course values ('CS-001', 'weekly seminar', 1, 1);

insert into course values ('CS-111', 'PBL', 1, 3);

insert into section (section_id, year) values ('8', 2027);

